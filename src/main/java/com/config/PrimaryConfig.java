package com.config;

/**
 * Basic Static retriever to provide base configuration. Will move to a protobuf serialization later with
 * non-embedded config.json.
 */
public class PrimaryConfig {

    private static String default_language = "EN_US";
}
